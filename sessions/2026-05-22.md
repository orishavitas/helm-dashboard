# Session Log - 2026-05-22

## Pickup

- Resumed from `sessions/2026-05-21.md` and `TODO.md`.
- Confirmed the active next step is the Sprint 02 authenticated browser visual check for `/` and `/projects/[id]`.
- Confirmed the Codex session monitor is already running and this repo still has no `.agent-harness` packet source.

## Work Performed

- Verified the local dev server on `http://127.0.0.1:3000/login` returns `200`.
- Found local OAuth generated `http://localhost:3006/api/auth/callback/google` because `.env.local` sets `AUTH_URL=http://localhost:3006`.
- Started a matching local dev server on port `3006` and verified `http://localhost:3006/login` returns `200`.
- Retried Google sign-in on local port `3006`; Google still rejected it with `Error 400: redirect_uri_mismatch`, so the local callback URI is not authorized in the Google OAuth client.
- Retried the production flow at `https://helm-dashboard-ten.vercel.app/`; production reaches the normal Google account sign-in page with redirect URI `https://helm-dashboard-ten.vercel.app/api/auth/callback/google`.

## Current Blocker

- Codex cannot complete the authenticated visual check without a human Google sign-in.
- Local sign-in additionally requires adding `http://localhost:3006/api/auth/callback/google` to the Google OAuth client's authorized redirect URIs, or changing `.env.local` and the dev port to a URI already authorized in Google Cloud.

## Next Safe Step

- Sign in through the Playwright/browser page currently at the Google account prompt for `https://helm-dashboard-ten.vercel.app`, then verify `/` and one `/projects/[id]` page for GitHub PR/commit state and grouped terminal presence.
- Alternative: authorize the local callback URI in Google Cloud, then run the same visual check against `http://localhost:3006`.
